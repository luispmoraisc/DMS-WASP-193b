{
  "$schema": "https://turborepo.com/schema.json",
  "tasks": {
    "build": {
      "dependsOn": [
        "^build"
      ],
      "inputs": [
        ".env*"
      ],
      "outputs": [
        "dist/**"
      ]
    },
    "lint": {
      "with": [
        "biomejs"
      ]
    }
  }
}